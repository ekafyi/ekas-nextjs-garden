# MDX Page!

```
const code = {}
```

_wow._ mdx page.

lorem ipsum dolor sit amet. Laravel provides an API middleware and routing out of the box, which enables us to set up REST APIs. We can then access the endpoints from our JavaScript layer, whether it’s vanilla JS or any frontend library/framework.

aa
