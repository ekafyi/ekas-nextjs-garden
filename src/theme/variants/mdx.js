import { h2, h3, h4, h5 } from "../typography";

export default {
  h2,
  h3,
  h4,
  h5,
  // p: mdxStyles.p,
  // ul: mdxStyles.ul,
  // ol: mdxStyles.ol,
  // li: mdxStyles.li,
  // inlineCode: mdxStyles.inlineCode,
  // blockCode: mdxStyles.blockCode,
};
