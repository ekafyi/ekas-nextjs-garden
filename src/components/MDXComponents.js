import { Styled } from "theme-ui";

// export const p = ({ children }) => {
//   return <Styled.p>{children}</Styled.p>;
// };

// export const h2 = ({ children }) => {
//   return <Styled.h2>{children}</Styled.h2>;
// };

// TODO doesnt work on custom content dir hhh
